```javascript
var pesel = arguments.Value;
var weights = [1, 3, 7, 9, 1, 3, 7, 9, 1, 3];
var sum = 0;

if (pesel.length !== 11) {
    arguments.IsValid = false;
} else {
    for (var i = 0; i < 10; i++) {
        sum += (pesel[i] * weights[i]);
    }
    sum = sum % 10;
    arguments.IsValid = (10 - sum) % 10 === parseInt(pesel[10]);
}
```

Jesteś tłumaczem języka angielskiego na polski.  

### Zasady działania:  
1. Przetłumacz poniższy tekst z angielskiego na polski.  
2. Jeśli fragment tekstu jest kodem programistycznym (np. zawiera składnię programowania, funkcje, zmienne lub bloki kodu), pozostaw go bez zmian.  
3. Zwróć wyłącznie przetłumaczony tekst oraz niezmienione bez żadnych dodatkowych informacji, komentarzy czy pytań. Tekst:



=====================

Sure, here is the JavaScript code for PESEL validation:

```javascript
var pesel = arguments.Value;
var weights = [1, 3, 7, 9, 1, 3, 7, 9, 1, 3];
var sum = 0;

if (pesel.length !== 11) {
    arguments.IsValid = false;
} else {
    for (var i = 0; i < 10; i++) {
        sum += (pesel[i] * weights[i]);
    }
    sum = sum % 10;
    arguments.IsValid = (10 - sum) % 10 === parseInt(pesel[10]);
}
```

Explanation:

PESEL is a Polish national identification number. It consists of 11 digits where the last digit is a check digit used to verify the correctness of the number.

The code first checks if the length of the PESEL number is 11. If not, it sets `arguments.IsValid` to `false`.

If the length is 11, it then calculates the sum of the products of the first 10 digits and their corresponding weights (as defined in the `weights` array). The weights are a predefined set of numbers used for the calculation.

The sum is then taken modulo 10, and subtracted from 10. The result is then taken modulo 10 again to get the check digit.

Finally, the code checks if the calculated check digit matches the last digit of the PESEL number. If they match, `arguments.IsValid` is set to `true`, otherwise it's set to `false`.